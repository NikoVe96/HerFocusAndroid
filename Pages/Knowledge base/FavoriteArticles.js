import { Text, View } from "react-native";

export const FavoriteArticles = () => {
    return (
        <View style={{ alignItems: 'center', justifyContent: 'center', flex: 1 }}>
            <Text>Favorite articles page</Text>
        </View>
    );

}

export default FavoriteArticles;